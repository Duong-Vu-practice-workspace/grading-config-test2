replicaCount: 1

image:
  repository: vucongtuanduong/grading-config-server
  tag: "v1.0.0"
  pullPolicy: Always

service:
  port: 8086

spring:
  profilesActive: stg

config:
  serverUrl: "http://config-server:8086"

secrets:
  dbSecret: "db-secret"

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
